import React from "react";
import { RoundedButton, MainContainer } from "../components/common";

const Home = () => {
  return <MainContainer>
      <RoundedButton>
          Add Tickets
      </RoundedButton>
  </MainContainer>;
};

export default Home;
